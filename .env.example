# ─── Notion (2 steps: create integration + share database) ───
NOTION_TOKEN=ntn_...
NOTION_SOURCE_DATABASE_ID=...
NOTION_DEST_DATABASE_ID=...

# ─── Google Sheets (only needed if using google_sheets backend) ───
# Uses service account JSON at ~/.config/gspread/service_account.json
# Sheet names configured in config.yaml

# ─── Claude AI (for live enrichment/dedup) ───
ANTHROPIC_API_KEY=sk-ant-...

# ─── Slack notifications (optional) ───
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../...
